package genericfs

import (
	"testing"
)

func TestSSHFS(t *testing.T) {
	t.Run("pass TestFS", func(t *testing.T) {
		//err := fstest.TestFS(nil, "dir1/file2", "file1")
		//assert.NoError(t, err)
	})
}
