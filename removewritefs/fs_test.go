package crudfs

import (
	"testing"
)

func TestCrudFS(t *testing.T) {

	t.Run("defaults to Open", func(t *testing.T) {
		//f, err := os.OpenFile("/tmp/adir", os.O_CREATE, fs.ModeDir)
		//defer f.Close()

		//assert.NoError(t, err)
	})

}
